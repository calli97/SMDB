class Role{
    constructor(tconst,nconst,category,job,characters){
        this.nconst=nconst
        this.tconst=tconst
        this.category=category
        this.job=job
        this.characters=characters
    }
}

module.exports=Role